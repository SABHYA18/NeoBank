package com.neobank.auth.model;

/**
 * User roles in the NeoBank system.
 * CUSTOMER — standard bank user
 * ADMIN    — internal admin with elevated privileges
 */
public enum Role {
    CUSTOMER,
    ADMIN
}
